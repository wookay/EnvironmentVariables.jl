```@raw html
<!-- generated -->
```
# Base Patches

### v"1.14.0-DEV.2857"

base/errorshow.jl

  * ```
    JULIA_STACKTRACE_FULL_LOADING
    ```

### v"1.14.0-DEV.2425"

base/precompilation.jl

  * ```
    JULIA_PRECOMPILE_JOBSERVER
    ```

  * ```
    JULIA_PRECOMPILE_JOBSERVER
    ```

  * ```
    JULIA_PRECOMPILE_THREADS
    ```

  * ```
    JULIA_IMAGE_THREADS
    ```

### v"1.14.0-DEV.2243"

base/loading.jl

  * ```
    JULIA_PRECOMP_REPORT_TIMING
    ```

### v"1.14.0-DEV.2014"

base/cmd.jl

  * ```
    JULIA_SHOW_ENV
    ```

### v"1.14.0-DEV.1963"

base/precompilation.jl

  * ```
    TERM
    ```

### v"1.13.0-DEV.1217"

base/client.jl

  * ```
    JULIA_TEST_VERBOSE
    ```

  * ```
    RUNNER_DEBUG
    ```

  * ```
    CI_DEBUG_TRACE
    ```

  * ```
    SYSTEM_DEBUG
    ```

### v"1.13.0-DEV.840"

base/terminfo.jl

  * ```
    TERM
    ```

### v"1.12.0-DEV.1244"

base/path.jl

  * ```
    WSL_DISTRO_NAME
    ```

### v"1.12.0-DEV.912"

base/client.jl

  * ```
    TERM
    ```

### v"1.12.0-DEV.612"

base/options.jl

  * ```
    FORCE_COLOR
    ```

  * ```
    NO_COLOR
    ```

### v"1.12.0-DEV.505"

base/logging/logging.jl

  * ```
    JULIA_DEBUG
    ```

### v"1.12.0-DEV.151"

base/util.jl

  * ```
    JULIA_TESTS
    ```

### v"1.12.0-DEV.128"

base/precompilation.jl

  * ```
    CI
    ```

  * ```
    JULIA_NUM_PRECOMPILE_TASKS
    ```

### v"1.11.0-DEV.897"

base/util.jl

  * ```
    JULIA_LOAD_PATH
    ```

### v"1.11.0-DEV.504"

base/client.jl

  * ```
    JULIA_FALLBACK_REPL
    ```

### v"1.11.0-DEV.307"

base/terminfo.jl

  * ```
    TERMINFO
    ```

  * ```
    TERMINFO_DIRS
    ```

  * ```
    COLORTERM
    ```

  * ```
    VTE_VERSION
    ```

  * ```
    XTERM_VERSION
    ```

  * ```
    TERMINAL_PROGRAM
    ```

  * ```
    KONSOLE_PROFILE_NAME
    ```

  * ```
    KONSOLE_DBUS_SESSION
    ```

  * ```
    TERM
    ```

### v"1.10.0-DEV.1294"

base/Base.jl

  * ```
    JULIA_WAIT_FOR_TRACY
    ```

### v"1.10.0-DEV.204"

base/loading.jl

  * ```
    JULIA_CPU_TARGET
    ```

  * ```
    JULIA_CPU_TARGET
    ```

### v"1.10.0-DEV.204"

base/linking.jl

  * ```
    JULIA_VERBOSE_LINKING
    ```

### v"1.8.0-DEV.1185"

base/util.jl

  * ```
    JULIA_PROJECT
    ```

### v"1.8.0-DEV.590"

base/util.jl

  * ```
    JULIA_DEPOT_PATH
    ```

### v"1.6.0-DEV.1167"

base/Base.jl

  * ```
    JULIA_MAX_NUM_PRECOMPILE_FILES
    ```

### v"1.6.0-DEV.354"

base/errorshow.jl

  * ```
    JULIA_STACKTRACE_EXPAND_BASEPATHS
    ```

  * ```
    JULIA_STACKTRACE_CONTRACT_HOMEDIR
    ```

  * ```
    JULIA_STACKTRACE_LINEBREAKS
    ```

### v"0.7.0-beta.280"

base/sysinfo.jl

  * ```
    JULIA_CPU_THREADS
    ```

### v"0.7.0-beta.280"

base/util.jl

  * ```
    JULIA_CPU_THREADS
    ```

### v"0.7.0-beta.90"

base/initdefs.jl

  * ```
    JULIA_PROJECT
    ```

### v"0.7.0-DEV.5168"

base/sysinfo.jl

  * ```
    PATH
    ```

### v"0.7.0-DEV.4898"

base/loading.jl

  * ```
    JULIA_LOAD_PATH
    ```

  * ```
    JULIA_LOAD_PATH
    ```

### v"0.7.0-DEV.3483"

base/initdefs.jl

  * ```
    JULIA_DEPOT_PATH
    ```

### v"0.4.0-dev+1914"

base/stream.jl

  * ```
    LINES
    ```

  * ```
    COLUMNS
    ```

### v"0.4.0-dev+1852"

base/client.jl

  * ```
    OLDPWD
    ```

### v"0.4.0-dev+1734"

base/initdefs.jl

  * ```
    JULIA_LOAD_PATH
    ```

### v"0.4.0-dev+1604"

base/threadcall.jl

  * ```
    UV_THREADPOOL_SIZE
    ```

### v"0.2.0-rc2+39"

base/client.jl

  * ```
    JULIA_SHELL
    ```

### v"0.2.0-rc1+173"

base/client.jl

  * ```
    SHELL
    ```
